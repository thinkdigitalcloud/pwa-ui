# Route the @thinkdigitalcloud scope to GitHub Packages.
# The auth token is read from the NODE_AUTH_TOKEN env var so no secret is
# committed. For local publishing:  export NODE_AUTH_TOKEN=<github-pat>
@thinkdigitalcloud:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
